<%= form_for(current_user.active_relationships.find_by(followed_id: @shop.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn gray-button" %>
<% end %>