<% provide(:title, 'All Shops') %>
<% provide(:user, @user) %>
<h1 class="lowercase">All Shops</h1>

<ul class="shops">
  <% @shops.each do |shop| %>
    <%= render shop %>
  <% end %>
</ul>

<%= will_paginate %>